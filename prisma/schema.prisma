generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//User model

id,email - unique,name,role - an enum which we will define,passwordHash,
